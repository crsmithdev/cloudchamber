# Google Drive for Desktop scratch
.tmp.drivedownload/
.tmp.driveupload/

# Python
__pycache__/
*.py[cod]

# Regenerable pipeline output. `pipeline harvest` rebuilds the pool from
# refs/ at any time. extracted/decisions.jsonl is DELIBERATELY NOT ignored —
# it is the one file here that cannot be regenerated from anything.
extracted/exemplars.jsonl
extracted/themes.jsonl
extracted/packets/
